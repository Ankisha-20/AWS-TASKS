## Response Mapping Template for createEvent Mutation

#if($ctx.error)
  $util.error($ctx.error.message, $ctx.error.type)
#elseif(!$ctx.result)  ## If there's no DynamoDB response
  $util.error("Failed to insert the event into DynamoDB", "DynamoDBError")
#else
  {
    "id": "$ctx.result.id.S",              ## UUID of the newly created event
    "createdAt": "$ctx.result.createdAt.S" ## CreatedAt timestamp
  }
#end